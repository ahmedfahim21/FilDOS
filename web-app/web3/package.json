{
  "name": "fildos-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for FilDOS - Filecoin Decentralized Storage",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:calibration": "hardhat ignition deploy ignition/modules/FILDOS.ts --network calibrationnet",
    "deploy:mainnet": "hardhat ignition deploy ignition/modules/FILDOS.ts --network filecoinmainnet",
    "verify:calibration": "hardhat verify --network calibrationnet",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.0.0",
    "@nomicfoundation/hardhat-ignition": "^0.15.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.25.0",
    "dotenv": "^16.0.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
